<% posts ||= [] %>
<ul>
<% posts.each do |post| %>
  <li>
   <%= post.name %>
   <ul>
   <% post.comments.each.do |comment| %>
     <li><%= link_to(comment.name, :action => 'show', :id => comment.permalink) %></li>
  <% end %>
</ul>
</li>
<% end %>
</ul>
