<h2><%= @post.name %></h2>
<p><%= @post.content %></p>
<ul>
<% @post.comments.each do |comment| %>
    <li>
	<p><%= comment.content %></p>
	<p><%= mail_to(comment.email,comment.author) %></p>
    </li>
     <% end %>
</ul>
