<h1><%= @post.name %></h1>
<p><%= @post.content %><p>
<ul>
<% @post.comments.each do |comment|%>
<li>
<p><%= comment.content %></p>
<%= link_to(comment.author, :action => 'mailto:#{comment.email}') %>
</li>
<% end %>
</ul>
